h3 收支类型管理
  .col-sm-2.pull-right
      select.form-control
        option 收入
        option 支出
  small Payment Type Management

.container-fluid(ng-controller="NestableController as nest")
  .row
    .col-md-1
    .col-md-4
      h4.text-left.Sub-heading 已存在的级联列表
    .col-md-2
    .col-md-4
      h4.text-left.Sub-heading 未分配的级联列表
    .col-md-1
  .row
    .col-md-1
    .col-md-4
      .panel.panel-default.well
        div(ng-nestable='', ng-model='nest.items')
          div
            | {{$item.text}}
    .col-md-2
      div
        p.text-left 请拖动编辑级联列表
        small.text-muted 拖动右边的未分配级联项到左侧级级联菜单
    .col-md-4
      .panel.panel-default.well
        .show-handlers(ng-nestable='', ng-model='nest.unuseitems')
          div
            | {{$item.text}}
        form(ng-submit='todo.addTodo(todo)').mb-xl
          .form-group
            input.form-control(ng-trim="true",name='amount', type='text', ng-model='todo.todo.amount', placeholder='添加级联项', required)
        button.btn.btn-primary.btn-block(type='submit') 添加
    .col-md-1

