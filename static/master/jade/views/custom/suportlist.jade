h3 审核队列
  small Check for apply

.container-fluid(ng-controller="SuportListController as table")
  // Editable row
  h4.page-header 申请列表

  table.table.table-bordered.table-hover.bg-white
    tr(style='font-weight: bold')
      td 团队名
      td 申请人
      td 申请时间
      td 开始时间
      td 停止时间
      td 申请地点     
      td 状态     
    tr(ng-repeat='apply in table.applys')
      td
        span {{ apply.team_name }}
      td
        span {{ apply.name }}
      td
        span {{ apply.add_time | NowTime }}  
      td
        span {{ apply.begin_time | NowTime }}
      td
        span {{ apply.end_time | NowTime }}
      td
        span {{ apply.place_name}}
      td
        span(ng-class='{"label label-info":apply.status==0,"label label-success":apply.status==1,"label label-danger":apply.status==2}') {{ apply.status | statusFilter }}
  p.text-center(ng-show='!table.applys||table.applys.length<1')
            |暂无任何数据

  